{
  "name": "bitbank-lab-mock",
  "version": "0.1.0",
  "description": "bitbank Private REST モック。注文状態を持つ挙動確認・検証用であり、公式テスト環境ではない。",
  "type": "module",
  "license": "MIT",
  "author": "tjackiet",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tjackiet/bitbank-mock-api.git"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "fastify": "^5.8.5",
    "undici": "^6",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@vitest/coverage-v8": "^4.1.11",
    "fast-check": "^4.9.0",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
